# Server 
PORT=3000
NODE_ENV=development

# JWT
# Replace with a long random string in production.
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-me-to-a-long-random-secret-string-please

